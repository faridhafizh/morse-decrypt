/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add allowedDevOrigins for local network testing if needed
  experimental: {
    // any experimental features can go here
  },
}

module.exports = nextConfig